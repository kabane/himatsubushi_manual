<h2><%= @user.full_name %></h2>

<table>
  <thead>
    <tr>
      <th>氏名</th>
      <th>性別</th>
      <th>生年月日</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @user.full_name %></td>
      <td><%= @user.gender %></td>
      <td><%= @user.birthday&.strftime('%Y/%m/%d') %></td>
      <td><%= link_to '編集', edit_admin_user_path(@user) %></td>
      <td><%= link_to 'アカウント編集', edit_admin_accounts_path %></td>
    </tr>
  </tbody>
</table>
